# rock build artefacts
build/
*.rock
*.oci

# model weights + gguf shards (downloaded/split into .cache/, injected at
# build, never committed)
.cache/
*.gguf

# spread scratch
spread.env
tests/spread/tmp/
