# rock build artefacts
build/
*.rock
*.oci
bonsai-1.7B.oci

# model weights (downloaded to .cache/, injected at build, never committed)
.cache/
*.gguf
*.gguf.part*

# go build output
/bin/
bonsai/*/bin/

# make stamps + spread scratch
*.stamp
.*.stamp
spread.env
tests/spread/tmp/
